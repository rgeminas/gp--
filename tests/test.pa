program HelloWorld;
    const
        a = 3;
        b = 2.;
        c = 3.4;
        d = true;
        
    var
        e, f, g : (* yes, they are integers *) integer;
        h, i : real;

    procedure ProcedureWithNoParams ;
        const
            a = 3;
            b = 2.;
            c = 3.4;
            d = true;
        var
            e, f, g : integer;
            h, i : real;
    begin
        while e mod 3 <> 2 do
        begin
            e := a + 2
        end;
    end;
    
    procedure ProcedureWithParams(a, b, c : integer; d, e, f : real);
        const
            q = 3;
        var
            g: integer;
    begin
        g := f or a;
        ProcedureWithNoParam;
        ProcedureWithParams(a, b + c, c, e, f, g)
    end;
begin
    h:=3
end.
